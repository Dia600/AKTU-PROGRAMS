#include<stdio.h>

int main()
{
    int n,rev=0,remain;
    printf("Enter the number:\n");
    scanf("%d",&n);
    printf("The reversed number is: ");
    while (n>0)
    {
        remain = n%10;
        rev = rev*10 + remain;
        n = n/10;
    }

    printf("%d",rev);
   
    return 0;
}
