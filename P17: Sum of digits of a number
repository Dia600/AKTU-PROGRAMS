#include<stdio.h>

int main()
{
    int n,sum=0,remain;
    printf("Enter the number:\n");
    scanf("%d",&n);
    printf("The sum of digits of number %d is:",n);
    while (n>0)
    {
        remain = n%10;
        sum += remain;
        n = n/10;
    }

    printf("%d",sum);
   
    return 0;
}
